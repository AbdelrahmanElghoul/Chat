<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:id="@+id/layout_receiver"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/average_margin_padding"
>

    <TextView
        android:id="@+id/txt_chat_receiver"
        style="@style/chat_text"
        android:text="message"
        android:background="@drawable/round_view_receiver"
       app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/txt_date_receiver"
        app:layout_constraintVertical_bias="0" />



    <TextView
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"

        android:layout_width="wrap_content"
        android:visibility="gone"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="22/2"
        android:id="@+id/txt_date_receiver"/>
</androidx.constraintlayout.widget.ConstraintLayout>